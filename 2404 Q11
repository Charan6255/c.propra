#include <stdio.h>

int main() {
    int m, n;
    printf("Enter the value of M and N: ");
    scanf("%d %d", &m, &n);

    for (int i = 1; i <= n; i++) {
        printf("%d x %d = %d\n", i, m, i * m);
    }

    return 0;
}
