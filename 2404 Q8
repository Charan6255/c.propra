#include <stdio.h>

int main() {
    int p, q, r;
    printf("Enter the values of P, Q, and R: ");
    scanf("%d %d %d", &p, &q, &r);

    printf("Numbers are = ");
    for (int i = p; i <= q; i++) {
        if (i % 10 == r || i == r) {
            continue;
        }
        printf("%d, ", i);
    }
    printf("\n");

    return 0;
}
